apiVersion: v1
kind: Service
metadata:
  name: {{ include "transaction_service.fullname" . }}
  labels:
    {{- include "transaction_service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "transaction_service.selectorLabels" . | nindent 4 }}
