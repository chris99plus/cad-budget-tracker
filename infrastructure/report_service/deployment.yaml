apiVersion: batch/v1
kind: CronJob
metadata:
  name: report-service-cronjob
  namespace: free
spec:
  schedule: "33 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          imagePullSecrets:
          - name: regcred
          containers:
          - name: report-service-cronjob
            image: report-service:latest
            imagePullPolicy: IfNotPresent
            env:
            - name: EXECUTION_MODE
              valueFrom:
                configMapKeyRef:
                  name: report-service-envs
                  key: executionMode
            - name: MONGODB_CONNECTION_STRING
              valueFrom:
                configMapKeyRef:
                  name: report-service-envs
                  key: mongodbConnectionString
          restartPolicy: Never